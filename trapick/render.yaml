services:
  - type: web
    name: trapick
    env: python
    buildCommand: |
      cd trapick
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: cd trapick && gunicorn trapick.wsgi:application